import ImportForm from "components/BookingComponent/BookingComponent";
import React from "react";

const Booking: React.FunctionComponent = (props) => {
  return (
    <div className="page">
      <ImportForm />
    </div>
  );
};

export default Booking;
